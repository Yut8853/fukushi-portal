# くらし支援ナビ（fukushi-portal-mvp）

自治体・福祉相談窓口・支援制度をCSVで管理するNext.js 15のMVPです。Reactコンポーネントや
TypeScriptへ自治体データを直接書かず、`data/` のCSVを追加・更新すると画面へ反映される構成です。
将来Supabaseへ移行できるよう、CSVの読み込み、Zod検証、表示用View Modelを分離しています。

このREADMEは、開発者または別のAIが現状を判断して作業を引き継ぐための資料も兼ねています。

## 現在の状態

最終確認日: **2026-07-27**

| 項目 | 状態 |
| --- | --- |
| 都道府県マスター | 47件 |
| ポータル登録自治体 | 1,076件（`standard` 963件、`basic` 113件） |
| 全国団体データ | 1,919件（現行自治体1,741件、政令市行政区171件、集計・特殊地域7件） |
| 全国マスター公式URL確定済み | 1,537 / 1,741自治体 |
| 全国マスター公式URL未確定 | 204自治体 |
| クロールキュー | 1,542件（ポータル側の確認済みURLを含む、`pending` 1,539件） |
| クロール処理済み | 3件（水戸市、つくば市、日立市） |
| クロール候補 | 51件、すべて`review_required`（CSV未反映） |
| ローカル承認フロー | 編集・確認・監査・CSV公開・失敗時復元を実装済み |
| 利用者向けUI | 状況→地域→案内の3段階、モバイル優先、選択状態と結果件数を明示 |
| 窓口データ | 4,109件（`published` 4,099件、`review_required` 10件） |
| 制度データ | 25件（全国共通17件、自治体独自8件） |
| 出典データ | 300件（すべて`published`） |
| `data:validate` | 成功。全国マスター公式URL未登録204件の警告1件 |
| `lint` | 成功 |
| `build` | 成功（Next.js 15.5.22） |
| `npm audit --omit=dev` | 本番依存の脆弱性0件 |

重要事項:

- 全国一括クロールは開始していません。水戸市、つくば市、日立市で限定試験を実施しました。
- 限定試験では水戸市15候補、つくば市11候補を抽出しました。つくば市はリンク切れPDFが
  1件あり`partial`、水戸市は`review_required`です。候補は重複・分類・連絡先を人間が確認するまで
  公開CSVへ反映しません。
- つくば市は公式一次情報で14窓口・自治体独自5制度を確認済みです。窓口13件は公開、
  受付時間等が未確認の社会福祉協議会1件は`review_required`です。
- 札幌市・横浜市・那覇市は、各市の公式サイトで代表窓口、生活困窮者自立相談、
  住居確保給付金、生活保護相談を確認し、各4窓口を公開しています。区ごとに連絡先が異なる
  札幌市・横浜市の窓口は電話番号を推測せず、公式の区別窓口一覧へ案内します。
- 仙台市・名古屋市・神戸市・福岡市も同じ4分類を公式一次情報で確認し、各4窓口を
  公開しています。住所によって担当区が変わる窓口は、市の公式一覧へ案内し、
  市全域で共通と確認できない電話番号は登録していません。
- 全国47都道府県の県庁所在地は、すべて`supportLevel=standard`です。既存8都市を除く
  39都市へ、市役所代表、生活困窮者自立相談、住居確保給付金、生活保護相談・申請の
  4導線、合計156窓口を公的な一次情報から追加しました。県庁所在地以外ではつくば市も
  `standard`です。
- 茨城県は県公式の市町村一覧、自立相談窓口一覧、生活保護申請窓口一覧を使い、全44市町村を
  `standard`化しました。市役所・町村役場、自立相談、住居確保給付金、生活保護の4導線を
  すべて公開し、県公式3出典の到達性と本文ハッシュを変更監視しています。
- 栃木県は全25市町を`standard`化しました。既存の宇都宮市を除く24市町へ、役所・役場、
  生活困窮者自立相談、住居確保給付金、生活保護の4導線、合計96窓口を追加しています。
  25市町すべての公式サイトへ到達し自治体名を照合済みです。県公式の窓口資料3件は
  到達性と本文ハッシュを変更監視しています。
- 群馬県は全35市町村を`standard`化しました。既存の前橋市を除く34市町村へ、役所・役場、
  生活困窮者自立相談、住居確保給付金、生活保護の4導線、合計136窓口を追加しています。
  県公式の2026年4～5月更新資料で全件を照合し、全国マスターで未確定だった10自治体の
  公式URLも確認しました。県公式3出典は到達性と本文ハッシュを変更監視しています。
- 埼玉県は全63市町村を`standard`化しました。既存のさいたま市を除く62市町村へ、
  役所・役場、生活困窮者自立相談、住居確保給付金、生活保護の4導線、合計248窓口を
  追加しています。県公式の市町村リンク、生活困窮者総合相談窓口、生活保護法上の
  福祉事務所一覧を照合し、3出典の到達性と本文ハッシュを変更監視しています。
- 千葉県は全54市町村を`standard`化しました。既存の千葉市を除く53市町村へ、役所・役場、
  生活困窮者自立相談、住居確保給付金、生活保護の4導線、合計212窓口を追加しています。
  県公式の市町村一覧、自立相談窓口一覧、生活保護相談窓口一覧を照合しました。県公式に
  住所の掲載がない自立相談窓口は推測せず空欄とし、3出典を変更監視しています。
- 神奈川県は全33市町村を`standard`化しました。既存の横浜市を除く32市町村へ、
  役所・役場、生活困窮者自立相談、住居確保給付金、生活保護の4導線、合計128窓口を
  追加しています。県公式の市町村役場一覧、自立相談支援機関、住居確保給付金、
  市部・町村部の生活保護窓口を照合し、5出典を変更監視しています。
- 新潟県は全30市町村を`standard`化しました。既存の新潟市を除く29市町村へ、役所・役場、
  生活困窮者自立相談、住居確保給付金、生活保護の4導線、合計116窓口を追加しています。
  県公式PDF2件をテキスト抽出と全ページ画像の両方で照合し、生活保護の市部・町村部の
  所管ルールも県公式ページで確認しました。3出典を変更監視しています。
- 富山県は全15市町村を`standard`化しました。既存の富山市を除く14市町村へ、役所・役場、
  生活困窮者自立相談、住居確保給付金、生活保護の4導線、合計56窓口を追加しています。
  県公式Excelの令和8年7月時点の自立相談窓口をセル単位で照合し、市町村所在地PDFは
  全ページを画像確認しました。選挙管理委員会直通番号を代表電話へ流用せず、未確認の
  代表電話28欄は空欄です。3出典を変更監視しています。
- 石川県は全19市町を`standard`化しました。既存の金沢市を県公式の令和8年度資料で更新し、
  残る18市町へ、役所・役場、生活困窮者自立相談、住居確保給付金、生活保護の4導線、
  合計72窓口を追加しています。自立相談支援機関PDFは文字抽出と全ページ画像の両方で確認し、
  町部の生活保護は県公式の所管ルールと福祉事務所一覧を照合しました。4出典を変更監視しています。
- 福井県は全17市町を`standard`化しました。既存の福井市を県公式資料で更新し、残る16市町へ
  主要4導線を追加しています。若狭町は旧三方町域と旧上中町域で自立相談・住居確保給付金・
  生活保護の担当センターが異なるため、地域別に分けて登録し、全県で67窓口を追加しました。
  市町連絡先PDFは全ページを画像確認し、4出典を変更監視しています。
- 山梨県は全27市町村を`standard`化しました。既存の甲府市を県公式資料で更新し、残る26市町村へ
  主要4導線を追加して、全県で104窓口を追加しました。市町村所在地は令和6年5月7日版PDFを
  画像確認し、自立相談・住居確保給付金・生活保護は県公式ページを照合しています。
  町村の自立相談窓口で住所が公表されていない欄は推測せず空欄とし、5出典を変更監視しています。
- 長野県は全77市町村を`standard`化しました。県公式の現行市町村一覧を全国マスターと自治体名で
  全件照合し、既存の長野市を更新、残る76市町村へ主要4導線を追加して304窓口を追加しました。
  まいさぽは19市と9広域所管、生活保護は19市と9福祉事務所の対応表に分け、PDF該当ページを
  画像確認しています。公表されていないまいさぽ住所は空欄とし、5出典を変更監視しています。
- 岐阜県は全42市町村を`standard`化しました。県公式の市町村一覧から42自治体の庁舎所在地、
  代表電話、公式URLを全件照合し、既存7自治体を更新、残る35自治体を追加しました。自立相談と
  住居確保給付金は21市と4広域窓口、生活保護は21市福祉事務所と5県所管へ正しく割り当て、
  主要4導線を全42自治体へ整備しました。最新パンフレットは画像でも確認し、5出典を変更監視しています。
- 静岡県は全35市町を`standard`化しました。県公式の市町所在地・リンク集と公式オープンデータを
  照合し、既存7自治体を更新、残る28自治体を追加しました。政令市の区別窓口や複数窓口を省略せず、
  代表35件、自立相談40件、住居確保給付金40件、生活保護43件の計158窓口を登録しています。
  県CSVの掛川市電話番号1件は県・市の別の公式ページで誤植を確認して補正し、6出典を変更監視しています。
- 愛知県は全54市町村を`standard`化しました。県公式の役所・役場一覧、2026年4月1日現在の
  自立相談支援機関PDF、住居確保給付金ページ、生活保護窓口一覧を照合し、既存8自治体を更新、
  残る46自治体を追加しました。名古屋市の地域別3自立相談窓口・22生活保護窓口、碧南市の
  自立相談2窓口などを省略せず、代表54件、自立相談57件、住居確保給付金57件、生活保護75件の
  計243窓口を登録しています。PDFは全2ページを画像でも確認し、公式出典6件を変更監視しています。
- 三重県は全29市町を`standard`化しました。県公式の市役所・町役場所在地、公式サイト一覧、
  自立相談支援機関PDF、2026年7月13日更新の住居確保給付金ページ、生活保護相談一覧を照合し、
  既存7自治体を更新、残る22自治体を追加しました。市部と多気町は各自治体の福祉事務所、
  残る14町は所管する県4福祉事務所へ割り当て、代表・自立相談・住居確保給付金・生活保護を
  各29件、計116窓口登録しました。PDFは画像でも全件確認し、公式出典6件を変更監視しています。
- 滋賀県は全19市町を`standard`化しました。県公式の市町役場一覧、県が直接案内する
  厚生労働省委託サイトの令和8年4月時点自立相談一覧、住居確保給付金の所管規則、
  生活保護相談・申請窓口一覧を照合し、既存7自治体を更新、残る12自治体を追加しました。
  大津市3窓口・高島市2窓口を省略せず、代表19件、自立相談22件、住居確保給付金22件、
  生活保護19件の計82窓口を登録しています。構造化されていない画像PDFは正データに使わず、
  県公式から遷移できる構造化一覧を使用し、出典5件を変更監視しています。
- 京都府は全26市町村を`standard`化しました。府公式の役所・役場一覧と公式サイトリンク、
  令和8年5月1日現在の自立相談支援機関、令和8年4月1日現在の福祉事務所一覧、
  京都市公式の生活保護申請案内を照合し、既存8自治体を更新、残る18自治体を追加しました。
  市部14福祉事務所、町村部を所管する府5保健所、京都市の区役所・支所案内を分け、
  代表・自立相談・住居確保給付金・生活保護を各26件、計104窓口登録しました。
  公式PDF2件は画像と文字座標の両方で確認し、出典7件を変更監視しています。
- 大阪府は全43市町村を`standard`化しました。府公式の市町村代表電話一覧、
  令和8年4月1日時点の自立相談支援機関一覧、令和8年4月1日現在の福祉事務所等一覧を照合し、
  既存16自治体を更新、残る27自治体を追加しました。大阪市24区、堺市7区、東大阪市3福祉事務所、
  豊中市2福祉事務所などを省略せず、代表43件、自立相談88件、住居確保給付金89件、
  生活保護77件の計297窓口を登録しています。公式PDF2件は全8ページを画像と文字座標で確認し、
  公式出典3件を変更監視しています。公式Excel中で全角文字を含み形式検証できないメール1件は
  推測補正せず空欄にしています。
- 兵庫県は全41市町を`standard`化しました。県公式の令和8年4月自立相談支援機関一覧と
  生活保護担当窓口一覧、市町庁舎案内を照合し、既存8自治体を更新、残る33自治体を追加しました。
  神戸市の地域別11自立相談窓口・12生活保護窓口、尼崎市の南北2窓口、町部12町を所管する
  広域窓口を分け、代表41件、自立相談64件、住居確保給付金64件、生活保護53件の
  計222窓口を登録しています。自立相談PDFは全3ページを画像と文字座標で確認し、
  補完した14自治体公式URLは到達と自治体名を照合、12町の代表電話も公式ページで確認しました。
  公式出典3件を変更監視しています。
- 奈良県は全39市町村を`standard`化しました。県公式の令和8年5月14日時点の
  市町村長・役場所在地一覧、令和8年2月更新の生活困窮者自立相談支援機関一覧、
  生活保護担当関係部局一覧を照合し、既存6自治体を更新、残る33自治体を追加しました。
  12市と十津川村は各自治体の自立相談・福祉事務所へ、その他の町村は
  中和・吉野の広域窓口へ所管地域どおりに割り当て、主要4導線を各39件、
  計156窓口登録しています。未確定だった9自治体の公式URLも公式サイトで確認し、
  県公式出典3件の到達性と本文変更を監視しています。
- 和歌山県は全30市町村を`standard`化しました。県公式の市役所・町村役場一覧、
  自立相談支援機関相談窓口、生活保護法上の福祉事務所一覧を照合し、既存6自治体を更新、
  残る24自治体を追加しました。9市は各市の自立相談・福祉事務所へ、21町村は
  7つの振興局健康福祉部・支所へ所管地域どおりに割り当て、主要4導線を各30件、
  計120窓口登録しています。未確定だった8自治体の公式URLも公式サイトで確認し、
  県公式出典3件の到達性と本文変更を監視しています。
- 鳥取県は全19市町村を`standard`化しました。県公式の市町村役場所在地録、
  自立相談支援機関相談窓口一覧、生活保護相談窓口・福祉事務所一覧を照合し、
  既存6自治体を更新、残る13自治体を追加しました。生活保護は各市町村福祉事務所へ、
  三朝町・大山町のみ現行の県中部総合事務所へ割り当て、主要4導線を各19件、
  計76窓口登録しています。未確定だった8自治体の公式URLも公式サイトで確認し、
  県公式出典3件の到達性と本文変更を監視しています。
- 島根県は全19市町村を`standard`化しました。県公式の市町村役場住所一覧、
  自立相談支援機関窓口一覧、県内各福祉事務所一覧を照合し、既存5自治体を更新、
  残る14自治体を追加しました。各市町村の自立相談窓口と福祉事務所を省略せず、
  主要4導線を各19件、計76窓口登録しています。未確定だった7自治体の公式URLも
  公式サイトで確認し、県公式出典3件の到達性と本文変更を監視しています。
- 岡山県は全27市町村を`standard`化しました。県公式の令和7年8月31日時点の
  市町村一覧、令和8年4月更新の自立相談支援機関、令和8年7月更新の
  住居確保給付金案内を照合し、既存7自治体を更新、残る20自治体を追加しました。
  15市と西粟倉村は各自治体の窓口へ、その他11町村は備前・備中・美作の
  3県民局へ所管どおりに割り当て、主要4導線を各27件、計108窓口登録しています。
  未確定だった7自治体の公式URLも公式一覧で確認し、公式出典4件を変更監視しています。
- 広島県は全23市町を`standard`化しました。県公式の令和7年4月1日時点の
  市区町一覧、令和7年5月更新の生活困窮者自立相談窓口、市・町福祉事務所一覧、
  居住支援協議会の住居確保給付金案内を照合し、既存6自治体を更新、残る17自治体を
  追加しました。各市町の自立相談窓口と福祉事務所を割り当て、主要4導線を各23件、
  計92窓口登録しています。未確定だった8自治体の公式URLも公式サイトで確認し、
  公式出典4件の到達性と本文変更を監視しています。
- 山口県は全19市町を`standard`化しました。県公式の市町庁舎所在地・連絡先、
  令和8年3月更新の自立相談支援機関一覧と住居確保給付金案内、生活保護・
  福祉事務所一覧を照合し、既存5自治体を更新、残る14自治体を追加しました。
  14市と周防大島町は各自治体の窓口へ、和木・上関・田布施・平生の4町は
  県柳井健康福祉センター・東部社会福祉事務所へ、阿武町は萩市の窓口へ
  所管どおりに割り当て、主要4導線を各19件、計76窓口登録しています。
  未確定だった6自治体の公式URLも県公式一覧で確認し、公式出典4件を変更監視しています。
- 徳島県は全24市町村を`standard`化しました。県公式の市町村一覧、生活困窮者
  自立相談支援事業窓口一覧、住居確保給付金の家賃・転居費用補助案内、
  福祉事務所一覧を照合し、既存5自治体を更新、残る19自治体を追加しました。
  8市は各市福祉事務所へ、16町村は東部・南部・西部の県福祉事務所へ
  管轄地域どおりに割り当て、主要4導線を各24件、計96窓口登録しています。
  未確定だった9自治体の公式URLも県公式一覧で確認し、公式出典4件を変更監視しています。
- 香川県は全17市町を`standard`化しました。県公式の市・町リンク集、令和7年7月
  更新の生活困窮者自立支援・住居確保給付金相談窓口、生活保護相談窓口を照合し、
  既存6自治体を更新、残る11自治体を追加しました。8市は各市福祉事務所へ、
  郡部9町は小豆総合事務所・東讃・中讃保健福祉事務所へ管轄どおりに割り当て、
  主要4導線を各17件、計68窓口登録しています。未確定だった5自治体の公式URLも
  県公式一覧で確認し、公式出典3件を変更監視しています。
- 2026-07-26に全国47都道府県へ分散して300自治体を追加しました。現行自治体であること、
  既存データと重複しないこと、公式ページへ到達できること、ページ上で自治体名と行政サイト
  であることを確認しています。窓口未調査の自治体は`basic`とし、詳細整備済み件数には数えません。
- ワーカー用の実在する運営者連絡先はローカルの`.env.local`へ設定済みです。
  `.env.local`はGitの追跡対象外で、メールアドレスをREADMEやCSVへ転記しません。
- 全国マスターで公式URLが未確定の204自治体は、推測URLを保存せず空欄のままにしています。
- 自動抽出した情報は公開せず、必ず`review_required`として人間のレビュー対象にします。
- 候補の`verified`化とCSVへの`published`反映は別操作です。公開前に担当者名と一次情報確認が必要です。
- `/admin/*`はBasic認証で保護されています。認証環境変数が未設定の場合は503で閉鎖します。
- 緊急連絡先、運営者名、訂正窓口は一般公開前に人間が表示内容を最終確認してください。
- Gitリポジトリは初期化済みで、`node_modules`と`.next`は追跡対象外です。
- パッケージ管理はnpmへ統一し、`package-lock.json`を正として使用します。

## セットアップ

Node.jsとnpmを用意し、プロジェクト直下で実行します。

```bash
npm install
cp .env.example .env.local
npm run dev
```

利用者向け画面は `http://localhost:3000/`、管理確認画面は以下です。

- `/admin/municipalities`: 自治体の登録状況、情報不足、期限切れ
- `/admin/review`: クロール候補のレビュー
- `/admin/crawl-jobs`: キュー、進捗、失敗理由
- `/admin/sources`: 取得した出典

### 公開・管理用の必須設定

```dotenv
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASSWORD=十分に長いランダムなパスワード
# 複数人運用:
# ADMIN_BASIC_USERS_JSON=[{"username":"admin-a","password":"...","role":"admin"},{"username":"reviewer-b","password":"...","role":"reviewer"}]
NEXT_PUBLIC_SITE_OPERATOR=運営者名
NEXT_PUBLIC_CORRECTION_EMAIL=訂正受付用メールアドレス
```

Basic認証は必ずHTTPS上で使用してください。`ADMIN_BASIC_PASSWORD`をGit、README、CSVへ
書き込まないでください。認証情報が未設定の場合、利用者向けページは表示されますが、
`/admin/*`は503で閉鎖されます。

`ADMIN_BASIC_USERS_JSON`を設定すると個人別アカウントを利用できます。`reviewer`は候補の編集・
確認、`admin`はそれに加えてCSV公開を実行できます。監査ログの確認者名はフォーム入力ではなく、
認証済みユーザー名を自動記録します。Basic認証には標準化されたログアウト機構がないため、
完全なセッション失効はSupabase Auth移行時に実装します。

## 利用者体験と安全設計

このサイトの成功基準はデータ件数ではなく、困っている人が少ない操作で適切な相談先へ
到達できることです。

- 全ページ上部に110、119、児童相談所虐待対応ダイヤル189、DV相談ナビ`#8008`を固定表示
- こころの危機には厚生労働省「こころの健康相談統一ダイヤル」を案内
- 制度名ではなく「今夜泊まる場所がない」「家賃が払えない」など生活状況から選択
- 公開画面は「困りごと」「地域」「案内を見る」の3段階を常に表示
- 困りごとの選択状態、選んだ状況、公開情報の件数を画面上で明示
- 地域選択は任意と明記し、個人情報を入力・保存しないことを検索前に表示
- 結果表示後は該当箇所まで自動スクロール
- 地域を選ばなくても、困りごとを1つ選べば全国共通制度を表示
- 自治体を選んだ場合だけ、その自治体の制度と公開済み窓口を追加表示
- 電話番号はスマートフォンから直接発信可能
- 各窓口・制度に情報確認日と公式出典を表示
- `/about`に目的、免責事項、現時点の個人情報の取り扱いを表示
- `/corrections`に訂正依頼の手順と運用フローを表示
- 17px以上のモバイル本文、スキップリンク、フォーカス表示、セマンティックHTMLを使用
- 管理画面には自治体、候補レビュー、クロール状況、出典を移動できる共通ナビを表示

緊急連絡先は自治体CSVに依存しない共通UIです。出典は警察庁、総務省消防庁、
こども家庭庁、内閣府男女共同参画局、厚生労働省の公式ページです。

注意:

- `#8008`は各相談機関の受付時間内に限られ、緊急の危険がある場合は110を案内します。
- こころの健康相談統一ダイヤルの受付時間は接続先地域によって異なります。
- 緊急表示の番号や案内条件は、一般公開前および定期的に一次情報で再確認してください。

## データ構造

`data/` のCSVが正規の元データです。

- `prefectures.csv`: 都道府県
- `municipalities.csv`: ポータルで扱う自治体
- `categories.csv`: 困りごとの分類
- `offices.csv`: 自治体ごとの相談窓口
- `programs.csv`: 全国・都道府県・自治体・民間の支援制度
- `municipality-programs.csv`: 自治体と制度の関連、地域別上書き
- `sources.csv`: 情報の出典
- `nationwide-municipalities.csv`: e-Statから同期した全国自治体・行政区と公式URL
- `emergency-contacts.json`: 全ページ共通の緊急連絡先、公式出典、確認日
- `crawl/queue.json`: 再開可能なクロールキュー
- `crawl/results/{都道府県コード}/{自治体コード}.json`: 自治体別の抽出結果

主な実装:

- `lib/csv.ts`: 共通CSVパーサー
- `lib/data/schemas.ts`: Zodスキーマと列挙値
- `lib/data/repository.ts`: CSVから元データを取得する層
- `lib/data/view-models.ts`: 画面表示用データへの変換
- `crawler/`: robots、取得、安全性検証、抽出、保存、ワーカー
- `scripts/`: データ追加、同期、URL解決、検証、クロール運用
- `supabase/schema.sql`: 将来移行用の初期スキーマ

CSVのヘッダー名は変更しないでください。複数値は「・」区切りです。日付は`YYYY-MM-DD`、
真偽値は`true`または`false`を使用します。電話番号やURLが不明な場合は空欄にできますが、
公開情報には出典と最終確認日が必要です。

## 公開ルール

一般利用者向け画面に表示されるのは、原則として次の条件を満たす情報だけです。

- `status=published`
- 公開に必要な必須項目が存在する
- 窓口・制度には有効な`sourceId`が存在する
- `lastVerifiedAt`が登録されている

自治体ステータス:

```text
draft / researching / review_required / verified / published / expired / suspended
```

`published`以外の自治体は一般利用者向け画面へ表示しません。詳細情報が不足していても画面を
エラーにせず、全国共通窓口と自治体公式サイトへ誘導します。

## データを追加する

### 自治体

対話形式で`municipalities.csv`へ`draft`として追加します。

```bash
npm run municipality:add
```

入力項目は都道府県、自治体名、自治体コード、自治体種別、公式サイト、代表電話、対応レベルです。
自治体コード、または同一都道府県内の自治体名が重複する場合は追加しません。

直接CSVを編集する場合も、最初は`draft`または`review_required`にしてください。一次情報で確認し、
出典と最終確認日を登録してから`published`へ変更します。

### 窓口・制度・出典

1. `sources.csv`へ公的な一次情報を登録する
2. `offices.csv`または`programs.csv`へ情報を登録する
3. `sourceId`で出典を関連付ける
4. 不明項目は推測せず空欄にする
5. 未確認項目がある場合は`status=review_required`にする
6. `npm run data:validate`を実行する

雛形は`import/`にもあります。

## 全国自治体マスターと公式URL

### e-Statから自治体一覧を同期

```bash
npm run municipalities:sync
```

`data/nationwide-municipalities.csv`を更新します。自治体コードの基礎データはe-Statの
市区町村コードを使用しています。同期後は既に確認済みの公式URLを失わないことを確認してください。

### 公式URLを解決

標準的な自治体ドメイン候補だけを検証する場合:

```bash
npm run municipality-urls:resolve
```

独自ドメインを検索候補から発見するフォールバックを有効にする場合:

```bash
URL_RESOLVER_SEARCH=1 URL_RESOLVER_CONCURRENCY=4 \
npm run municipality-urls:resolve
```

特定自治体だけを確認する場合:

```bash
URL_RESOLVER_SEARCH=1 URL_RESOLVER_CONCURRENCY=1 \
URL_RESOLVER_CODES=082015,012211 \
npm run municipality-urls:resolve
```

URL解決処理は次を行います。

- 自治体名の読みから標準的な`.lg.jp`等の候補を生成
- 検索フォールバックでは検索結果を候補発見にのみ使用
- HTTPS、DNS、プライベートIP、リダイレクト先を検証
- ページ本文、タイトル、自治体名相当のドメインを照合
- 検証できたURLだけを`officialUrl`へ保存

注意: 検索フォールバックは完全な公式性保証ではありません。独自ドメインは誤認余地があるため、
J-LIS、都道府県公式サイト、自治体ページの運営者表示などを使った人間の最終確認が必要です。
過去の試験で、ふるさと納税ポータルを自治体サイトと誤認するケースを検出し、該当URLを削除して
ドメイン検証を強化しています。

### 確認済み自治体をポータルへ一括追加

全国マスターから、公式ページへ実際に到達できた自治体だけを`basic`として追加します。

```bash
npm run municipalities:promote -- --count=300 --verified-at=2026-07-26
```

この処理は次の条件をすべて満たす自治体だけを採用します。

- `entityType=municipality`かつ`lifecycleStatus=current`
- 自治体コードとIDがポータル既存データと重複しない
- 公式URLが全国マスターで確定済み
- HTTPS/DNS/リダイレクト先が安全性検証を通過する
- 取得したHTMLに自治体名と行政サイトを示す表示が存在する
- 47都道府県を順番に巡回し、特定地域だけに偏らない

指定件数を確認できなければCSVへ一件も書き込みません。追加行は`supportLevel=basic`となり、
代表電話、個別窓口、自治体独自制度を確認済みとは扱いません。詳細情報は公式一次情報で
別途確認し、`sources.csv`と関連付けて追加してください。

## 全国クロール

### キュー作成

```bash
npm run crawl:queue -- --all
npm run crawl:queue -- --prefecture=08
npm run crawl:queue -- --municipalities=082015,082201
```

公式URL未登録自治体はキューへ入りません。既存キューへ重複なく追加され、停止後も再開できます。

### 環境変数

`.env.example`:

```dotenv
CRAWLER_CONTACT=your-email@example.com
CRAWLER_CONCURRENCY=2
CRAWLER_DELAY_MS=2000
CRAWLER_TIMEOUT_MS=15000
CRAWLER_MAX_RETRIES=3
CRAWLER_MAX_PAGES=20
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASSWORD=replace-with-a-long-random-password
NEXT_PUBLIC_SITE_OPERATOR=運営者名
NEXT_PUBLIC_CORRECTION_EMAIL=correction@example.com
```

`CRAWLER_CONTACT`は必須です。連絡先未設定のまま`crawl:worker`を実行すると、意図どおりエラーで
停止します。実在しないメールアドレスや仮の連絡先で本番クロールを開始しないでください。

設定可能範囲:

- 並列数: 1〜8
- リクエスト間隔: 500〜60,000ms
- タイムアウト: 1,000〜120,000ms
- 最大リトライ: 0〜3
- 自治体あたり最大ページ数: 1〜100

### ワーカー実行

```bash
export CRAWLER_CONTACT=operator@example.com
export CRAWLER_CONCURRENCY=2
export CRAWLER_DELAY_MS=2000
npm run crawl:worker -- --municipalities=082015,082201
```

静的HTMLを優先し、同一公式サイト内の福祉関連リンクとPDFを既定で最大20ページ取得します。
robots.txtを尊重し、localhost、プライベートIP、クラウドメタデータIP、認証情報付きURL、
非HTTPスキームを拒否します。リダイレクト先も再検証します。

誤って全国キューを一括処理しないよう、ワーカーは対象指定が必須です。

```bash
npm run crawl:worker -- --municipalities=082015,082201
npm run crawl:worker -- --prefecture=08 --limit=5
npm run crawl:worker -- --all
```

`--all`は全国実行を明示する場合だけ使用してください。既存キューが全国分でも、
`--municipalities`または`--prefecture`を指定すれば対象外のジョブは変更されません。

対象キーワードには福祉、生活、相談、支援、給付、困窮、生活保護、障害、介護、子育て、
ひとり親、DV、虐待、債務、法律などが含まれます。

自動抽出結果は例外なく`review_required`です。自動的に`published`にはなりません。

### 状態確認・再実行

```bash
npm run crawl:status
npm run crawl:report
npm run crawl:retry-failed
npm run crawl:retry-failed -- --municipalities=082201
npm run crawl:retry-failed -- --municipalities=082201 --force
```

停止後は同じ`crawl:worker`で未処理・リトライ待ちから再開できます。失敗・部分成功だけを
再投入する場合は`crawl:retry-failed`を使用します。`--municipalities`はカンマ区切りの
自治体コード、`--prefecture`は都道府県コードです。`--force`は既存結果を再取得するための
指定で、人間がレビュー済みの候補は上書きしません。対象指定なしの`--force`は拒否され、
全国を明示する場合だけ`--all`が必要です。

2026-07-26の限定試験結果:

```text
総数: 1316 / 処理済み: 3 / 残件: 1313 / 進捗: 0.2%
水戸市: review_required、候補15件
つくば市: partial、候補11件、リンク切れPDF 1件
日立市: review_required、候補25件
候補合計: 51件（HTML/PDF、全件review_required）
```

限定試験で、CMSの別経路にある同一ページ、PDF内の識別番号を電話番号と誤認するケース、
途中ページの404、Dropbox上に残るロックを検出しました。現在は同一内容の候補を統合し、
区切りのない10〜11桁の数字を電話番号として採用せず、途中ページの失敗を記録して残りを継続し、
排他的ファイルロックを終了時に解除します。抽出結果は候補発見の補助であり、正データではありません。

クロール状態:

```text
pending / running / completed / partial / failed / retry_waiting
review_required / skipped / blocked_by_robots
```

### 候補のレビューとCSV公開

`/admin/review`では、クロール候補について次を行えます。

- 原文と出典URLの確認
- 公開先（相談窓口・支援制度）と困りごとの分類の選択
- 名称、部署、連絡先、受付時間、対象者、金額、必要書類等の修正
- 確認者名と確認メモの記録
- 確認済み、却下、保留、再調査の状態変更
- `verified`候補だけをCSVへ公開

編集後の候補は自動公開されません。最初に「確認済みにする」を実行し、内容を再確認した後、
独立した「CSVへ公開する」を実行します。公開時は次を自動的に行います。

1. 自治体、分類、出典、必須項目、Zodスキーマを確認
2. 同じ自治体内の名称・電話番号・制度名から重複候補を検出
3. `sources.csv`の同一URLを再利用し、なければ新規出典を生成
4. `offices.csv`または`programs.csv`へCSVエスケープして追記
5. `data/crawl/backups/`へ変更前CSVと公開内容を保存
6. 全ポータルCSVを再読込して検証
7. 失敗した場合は変更前CSVを復元
8. `data/crawl/verification-log.json`へ担当者と操作履歴を保存

CLIで差分だけを確認する場合:

```bash
npm run crawl:diff -- \
  --municipality=082201 \
  --candidate=候補ID
```

CLIで公開する場合は、差分確認後に担当者名と明示的な`--confirm`を指定します。

```bash
npm run crawl:publish -- \
  --municipality=082201 \
  --candidate=候補ID \
  --actor=確認担当者名 \
  --confirm
```

公開・復元処理のfixtureテスト:

```bash
npm run crawl:publish:test
```

クロールが検出する内部分類は公開CSVの状況別分類へ変換されます。分類は自動提案に過ぎないため、
レビュー時に利用者の困りごととして適切か必ず確認してください。

## 検証

CSV編集やコード変更後は、最低限次をすべて実行します。

```bash
npm run data:validate
npm run lint
npm run build
```

`data:validate`は以下を確認します。

- 必須項目
- 自治体コードの重複
- 都道府県、自治体、窓口、出典IDの参照整合性
- URL、電話番号、日付形式
- 列挙値
- 公開情報の出典と最終確認日
- `published`情報の公開必須項目
- 全国自治体データの件数と公式URL未登録数

`standard`以上の自治体に主要4導線があるか、公開窓口の項目充足率、確認期限を監査します。

```bash
npm run data:quality
```

確認期限は現在180日です。管理画面の「情報不足あり」には、電話番号、住所、受付時間、
休業日、担当部署などの未登録項目も反映されます。

公開出典の到達性と本文ハッシュを監視し、公式ページの変更を検出します。

```bash
npm run sources:monitor -- --sources=source-ibaraki-municipalities,source-ibaraki-self-reliance-list
npm run sources:monitor -- --limit=10
```

安全のため、対象指定なしでは実行しません。監視結果は`data/source-monitor.json`へ保存され、
前回から本文が変わると`changed`になります。変更を検出しても自動公開せず、人間の再確認対象とします。

エラーがある場合は終了コード1、確認を要するが処理可能な内容は警告として表示されます。

2026-07-27時点の期待結果:

```text
エラー: 0件
警告: 1件
現行市区町村の公式URLが204件未登録
```

## Supabase移行時の変更箇所

通常は`lib/data/repository.ts`がローカルCSVを読み込みます。Supabase移行で残る作業は次のとおりです。

1. `supabase/schema.sql`を実プロジェクトへ適用する
2. `npm run supabase:seed -- --apply`でCSVを投入する
3. 本番Secretと`DATA_BACKEND=supabase`を設定し、読み込み試験を行う
4. Basic認証をSupabase Authへ置き換え、完全なログアウトを追加する
5. クロール結果JSONをDBまたはオブジェクトストレージへ移す
6. レビュー承認処理と監査ログをDBトランザクションへ移す

View Modelと画面はリポジトリ層を介しているため、CSV固有処理をUIへ持ち込まない方針です。

`supabase/schema.sql`は現在のCSV列、公開ステータス、管理者プロファイル、役割別RLS、
検証ログ、出典変更ログに対応しています。SupabaseへスキーマとCSVを投入後、サーバー環境で
次を設定すると、同じView Modelのまま読み込み先を切り替えられます。

```dotenv
DATA_BACKEND=supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=サーバー専用キー
```

Service Role Keyはブラウザへ公開せず、`NEXT_PUBLIC_`を付けないでください。設定不足やDB応答の
型不一致は明示的なエラーとなり、古いCSVへ黙ってフォールバックしません。

投入件数だけを確認するドライランと、実投入コマンドは次のとおりです。

```bash
npm run supabase:seed
npm run supabase:seed -- --apply
```

`--apply`は明示指定が必要で、都道府県、分類、自治体、出典、窓口、制度、自治体制度の順に
500件ずつupsertします。

## 未実装・既知の制約

- 全国1,741自治体のうち全国マスター上の204自治体は公式URL未確定
- 全国一括クロールは未開始（水戸市・つくば市・日立市の限定試験のみ実施）
- JavaScriptレンダリング必須サイトへのブラウザクロールは未実装
- Excel/CSVは型に含まれるが、現ワーカーの中心はHTMLとPDF
- OCRが必要な画像PDFへの対応は未実装
- ローカルCSVへの承認反映は実装済み。複数管理者による同時編集とDBトランザクションは未実装
- 管理画面はBasic認証。個人別アカウント、`admin/reviewer`権限、CSV公開監査ログは実装済み。
  セッション失効を伴うログアウトとDB上のアカウント管理はSupabase Auth移行まで未実装
- Supabase用スキーマと読み込みリポジトリは実装済み。本番プロジェクト作成、CSV投入、
  Auth切り替え、接続試験は外部Supabase環境が未設定のため未実施
- 全国規模での負荷試験、長時間連続運転、失敗率測定は未実施
- 公式URL検索フォールバックには人間による最終監査が必要
- 本番依存の監査は0件。開発依存を含む通常の`npm audit`には13件の高リスク警告が残る
- 訂正窓口はメール起動のみで、受付チケット・進捗管理・SLAは未実装
- スマートフォン実機と実際のスクリーンリーダーによる最終検証は未実施

## 次の担当者・AIへの推奨手順

1. このREADMEと`package.json`、`lib/data/schemas.ts`、`middleware.ts`、`crawler/security.ts`を読む
2. `npm run data:validate && npm run lint && npm run build`で基準状態を再確認する
3. 管理認証、運営者名、訂正窓口メールを本番環境へ設定する
4. スマートフォン、キーボード、スクリーンリーダー、低速回線で利用者導線を確認する
5. 緊急連絡先と免責表示を公的な一次情報・法務観点で最終確認する
6. 全国マスターの公式URL1,537件をJ-LISまたは都道府県公式一覧と照合する
7. 未確定204件を一次情報で補完する
8. 設定済みの`CRAWLER_CONTACT`を本番環境のSecretにも登録する
9. 水戸市・つくば市・日立市の51候補を管理画面で一次情報と照合し、重複・分類・電話番号を確認する
10. 確認済み候補だけをCSVへ公開し、利用者画面で出典と確認日を目視する
11. 次に茨城県の未処理市町村を少数件ずつ試験し、失敗率、取得ページ数、レビュー所要時間を計測する
12. OCR・JavaScript描画対応より前に、静的HTML/PDF候補の承認運用が継続可能か判断する

クロール開始前に、対象自治体数、並列数、待機時間、連絡先、保存容量、停止方法を必ず確認してください。
# fukushi-portal
